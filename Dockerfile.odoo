FROM odoo:19.0
USER root
# qifparse is not in the official image; isolate it so it cannot break system Python
RUN pip3 install --break-system-packages --target=/opt/qiflibs qifparse
ENV PYTHONPATH="/opt/qiflibs:$PYTHONPATH"
USER odoo
